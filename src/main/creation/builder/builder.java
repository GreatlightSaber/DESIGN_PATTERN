package main.creation.builder;

/*
 * 빌더패턴 (Builder Pattern)
 *  객체의 생성 과정을 객체의 속성설정부분과 객체의 실제적인 생성부분으로 나누는 방법
 *  
 *  * 장점
 *  	- 객체를 만드는 방법을 따로 둠으로서, 객체의 생성과정과 실제 생성부분을 분리할 수 있다. -> 관리가 용이하다.
 *  	- 코드가 깔끔해진다. -> 복잡한 속성의 객체를 단 한라인에도 생성할수 있게 된다.
 *  	- 볼변객체를 만들수가 있다.-> 일반적인 객체는 불변객체화 하기 어렵다
 * 
 * */

public class builder {
	
}
